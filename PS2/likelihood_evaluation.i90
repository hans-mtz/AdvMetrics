# 1 "/Volumes/SSD Hans 1/Github/AdvMetrics/PS2/likelihood_evaluation.f90"
MODULE LIKELIHOOD_EVALUATION
  USE GLOBVAR
! USE INIT
  USE PROBABILITY, ONLY : PDF_NORMAL, CDF_NORMAL
  IMPLICIT NONE

CONTAINS

  REAL(8) FUNCTION LIKE_EVAL(i)
    IMPLICIT NONE
    INTEGER, INTENT(IN) :: i
    REAL(8) :: xb

    LIKE_EVAL = 0.0d0
    xb = -DOT_PRODUCT(x(i,:),beta)
    LIKE_EVAL=CDF_NORMAL(xb,0.0d0,1.0d0)
  END FUNCTION LIKE_EVAL

END MODULE LIKELIHOOD_EVALUATION
